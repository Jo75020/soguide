<div class="padded-little"></div>
<div class="padded-little"></div>
<div class="container">
  <div class="row" style="text-align: center;">
    <div class="col-md-12 col-xs-12">
        <h3 class="title-index">Choisissez votre destination et découvrez nos guides !</h3>
        <p class="sub-title-index">Nous vous proposons de choisir un de nos 100 guides parmi 16 destinations. Sélectionnez un pays pour avoir des informations essentielles comme connaître la meilleure période pour le visiter.</p>
          <div class="padded-little"></div>

          <% @results.order("name").each do | country | %>

            <div class="col-md-3 col-xs-12 card-wrapper country">
              <div class="card" style="background-image:
         url('<%= cl_image_path country.photos.first.path, height: 300, width: 400, crop: :fill %>')">
                <div class="card-category">
                  <% @guides = Guide.where(pays: country.name) %>
                  <p> <%= country.name %> </p>
                </div>
                <div class="card-description">
                </div>
                <%= link_to "", country_path(country), class:"card-link" %>
              </div>

      </div>
<% end %>
      </div>
</div>
</div>

<% content_for(:after_js) do %>
  <%= javascript_tag do %>
      $(document).ready(function() {
      $('navbar-wagon-link').css({"color":"#303030"});
      $('navbar-wagon-link').css({"color":"#303030"});


      });
  <% end %>
<% end %>
